// approach to tackling the problem

// talk about the big problem

// read specs / requirements

// try to come up with a solution

function addArray(myArray, start) {
  if (!Array.isArray(myArray)) {
    return false;
  }
  if (!start) {
    start = 0;
  } return start


}


// possible solution: a function that takes arrays

// make sure it is an array

// maybe it is a long string

// str.split(', '); - splits the strings in the array

// calculates the sum of all changes

// code the solution

// node debug arrays.js

// Number(myArray[i]) - converts string to number

// var data = `

// data.split(`/n)



